# QrInterface
use parameters to make things, quizes and others on a QR code.
<pre>?mode=
ask
confirm
mensage
?text=
text displayed
?close=
text displayed when user type an incorrect password or something else.
?tab=
URL when user sucess the code, can be another link with mensages
</pre>
